✅ Stack: Next.js 14 + Tailwind CSS (authored as new frontend)
✅ Stil basiert auf /Restaurent + Inhalte aus old-site.txt
✅ Bilder aus /Nguyen-Restaurent übernommen und platziert
✅ deploy/install.sh aktualisiert (Next.js Build + Start)

FERTIGE SEITEN:
- /            → Homepage (Hero-Slider, Story, Menü-Preview, Galerie, Besuch)
- /menu        → Speisekarte mit Warenkorb
- /checkout    → Checkout, Gutschein-Einlösung, PayPal-Redirect
- /coupon      → Gutschein prüfen & kaufen (+10% Bonus)
- /thank-you   → Bestellbestätigung (dynamisch mit /thank-you/:id)
- /admin       → Dashboard mit 9 Tabseiten + Login

FUNKTIONS-BAUSTEINE:
- Globale Cart-Logik inkl. Drawer & LocalStorage
- Gutschein- & Geschenkcode-Anbindung an Backend
- PayPal Checkout Flow (API /api/checkout & Rückleitung)
- Hero-Slider, Preloader, Parallax, Responsive Navbar

ENV-VARIABLEN:
- PORT=5173
- NEXT_PUBLIC_BACKEND_URL=http://localhost:8080 (oder Server-URL)